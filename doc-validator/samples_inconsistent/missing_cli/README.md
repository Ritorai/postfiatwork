# Orphaned README

This directory documents a CLI that was never actually written. There is no
`.py` file here at all -- this triggers DOC008_NO_CLI.

## Rerun commands

```
python3 ghost.py --help
```
