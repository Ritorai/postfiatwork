#!/usr/bin/env bash
# Fixture regenerator for the DOC009 valid control. Never executed by
# docval -- the safety gate refuses every non-python3 launcher -- but it
# must EXIST, which is the entire point of this fixture.
set -u
echo "capture.sh: nothing to capture in a fixture"
